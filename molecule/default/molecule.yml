---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: centos:9
    command: /sbin/init
    tmpfs:
      - /run
      - /tmp
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
