---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: dokken/centos-stream-8
    command: /sbin/init
    tmpfs:
      - /run
      - /tmp
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
